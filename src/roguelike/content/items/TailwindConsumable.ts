import { ItemDefinition } from '../../registry/types';

export const TailwindConsumable: ItemDefinition = {
  id: 'tailwind',
  label: 'item.tailwind',
  rarity: 'rare',
  // No slot means it's a consumable
};
