/**
 * Global game constants (non-visual).
 */

export const GAME_CONSTANTS = {
  // Physics / Gameplay
  DEFAULT_FTP: 200,
  DEFAULT_RIDER_MASS_KG: 75,

  // Roguelike
  STARTING_GOLD: 0,
  COURSE_GENERATION: {
    DEFAULT_DISTANCE_KM: 5,
    DEFAULT_GRADE: 0.05,
  },

  // UI / Layout Ratios (Non-theme related)
  UI: {
    HUD_HEIGHT_RATIO: 0.1,
    PANEL_WIDTH_RATIO: 0.3,
  }
};
